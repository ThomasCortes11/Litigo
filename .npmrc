# Evita conflictos de peer dependencies entre next-auth y Next.js.
# Este archivo es necesario porque next-auth 5 beta declara un peer
# mas estricto que el que Vercel/npm resuelven automaticamente.
legacy-peer-deps=true
